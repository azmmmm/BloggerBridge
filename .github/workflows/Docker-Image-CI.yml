name: Docker Image CI

on:
  pull_request:
    branches: ["main","dev"]
  workflow_call:

jobs:

  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker compose --env-file ./config/docker-compose.env build
